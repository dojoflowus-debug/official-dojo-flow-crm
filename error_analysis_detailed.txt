Error Analysis from User Screenshot (Detailed):

The screenshot shows the Add Student modal with a student "Ashley Boyd" being added.
On the RIGHT SIDE of the screen, there's a long text string that appears to be:

1. The error text contains what looks like Base64 or URL-encoded data
2. At the bottom of the error text, I can see readable fragments:
   - "youth_program"
   - "4101 Nasa Parkway, 122,El Lago,TX,77586"
   - "113193532"
   - "2Q=="

This pattern suggests:
1. A tRPC query or mutation response is being displayed raw/unformatted
2. The data appears to be serialized student information
3. This could be a JWT token or serialized session data being displayed

Possible causes:
1. A tRPC query is returning raw data that's being displayed in the UI
2. An error response is being shown without proper parsing
3. A debug/logging statement is outputting to the page
4. A component is rendering raw query data instead of formatted content

The error appears on the RIGHT side of the modal, which suggests:
- It might be in a sidebar or overlay component
- Could be related to the student list/details panel
- Might be a toast notification gone wrong
- Could be related to the map or student card overlay

Need to check:
1. StudentCardOverlay component
2. MapOverlay component  
3. Toast notifications
4. Any component that renders on the right side of the Students page
