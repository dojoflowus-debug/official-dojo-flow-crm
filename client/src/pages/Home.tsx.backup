import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { APP_LOGO, APP_TITLE, getLoginUrl } from "@/const";
import { 
  UserCheck, 
  UserPlus, 
  FileSignature, 
  CreditCard,
  Calendar,
  ShoppingBag,
  Users,
  MessageSquare,
  BarChart3,
  Megaphone,
  Bot,
  X,
  LogIn,
  LayoutDashboard
} from "lucide-react";
import { useNavigate } from "react-router-dom";
import { useState, useEffect } from "react";

/**
 * DojoFlow Enhanced Kiosk - Main Navigation Screen
 * Features: Check-In, Registration, Waiver, Payment, Events, Shop, Referrals, Feedback, Analytics, AI Assistant
 */
export default function Home() {
  // The userAuth hooks provides authentication state
  // To implement login/logout functionality, simply call logout() or redirect to getLoginUrl()
  let { user, loading, error, isAuthenticated, logout } = useAuth();

  const navigate = useNavigate();
  const [showKai, setShowKai] = useState(false);
  const [announcements, setAnnouncements] = useState<any[]>([]);
  const [showStaffAnalytics, setShowStaffAnalytics] = useState(false)
  const [staffPin, setStaffPin] = useState("")
  const [showStaffDashboardPin, setShowStaffDashboardPin] = useState(false)
  const [staffDashboardPin, setStaffDashboardPin] = useState("")
  const [isValidatingPin, setIsValidatingPin] = useState(false)

  const validatePinMutation = trpc.staff.validatePin.useMutation()

  // Load active announcements
  useEffect(() => {
    // Mock announcements for now - will connect to backend API
    setAnnouncements([
      {
        id: 1,
        title: "🎉 Spring Camp Registration Open!",
        message: "Sign up for our Spring Break Camp - Limited spots available!",
        type: "promotion"
      }
    ]);
  }, []);

  const mainActions = [
    {
      title: "Check In",
      description: "Scan QR or enter phone",
      icon: UserCheck,
      path: "/checkin",
      color: "from-red-600 to-red-700",
      badge: null
    },
    {
      title: "I'm New",
      description: "Sign up for trial class",
      icon: UserPlus,
      path: "/new-visitor",
      color: "from-blue-600 to-blue-700",
      badge: "Popular"
    },
    {
      title: "Sign Waiver",
      description: "Complete digital waiver",
      icon: FileSignature,
      path: "/waiver",
      color: "from-purple-600 to-purple-700",
      badge: null
    },
    {
      title: "Make Payment",
      description: "Pay dues or fees",
      icon: CreditCard,
      path: "/payment",
      color: "from-green-600 to-green-700",
      badge: null
    },
    {
      title: "Events & Camps",
      description: "Browse & register",
      icon: Calendar,
      path: "/events",
      color: "from-orange-600 to-orange-700",
      badge: "New"
    },
    {
      title: "Shop Gear",
      description: "Uniforms & equipment",
      icon: ShoppingBag,
      path: "/shop",
      color: "from-pink-600 to-pink-700",
      badge: null
    },
    {
      title: "Refer a Friend",
      description: "Earn rewards",
      icon: Users,
      path: "/referral",
      color: "from-cyan-600 to-cyan-700",
      badge: null
    },
    {
      title: "Feedback",
      description: "Rate your experience",
      icon: MessageSquare,
      path: "/feedback",
      color: "from-indigo-600 to-indigo-700",
      badge: null
    }
  ];

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 flex flex-col">
      {/* Announcements Banner */}
      {announcements.length > 0 && (
        <div className="bg-gradient-to-r from-red-600 to-orange-600 text-white py-3 px-6 text-center">
          <div className="flex items-center justify-center gap-2">
            <Megaphone className="h-5 w-5" />
            <span className="font-semibold">{announcements[0].title}</span>
            <span className="hidden md:inline">• {announcements[0].message}</span>
          </div>
        </div>
      )}

      {/* Header */}
      <header className="border-b border-slate-800 bg-slate-950/50 backdrop-blur-sm">
        <div className="container mx-auto px-6 py-6 flex items-center justify-between">
          <div className="flex items-center gap-4">
            {APP_LOGO && (
              <img 
                src={APP_LOGO} 
                alt={APP_TITLE} 
                className="h-12 w-auto"
              />
            )}
            <div>
              <h1 className="text-2xl font-bold text-white">{APP_TITLE}</h1>
              <p className="text-sm text-slate-400">Self-Service Kiosk</p>
            </div>
          </div>
          
          <div className="flex items-center gap-2">
            <Button
              variant="outline"
              onClick={() => navigate('/student-login')}
              className="bg-blue-600 hover:bg-blue-700 text-white border-0"
            >
              <LogIn className="h-5 w-5 mr-2" />
              Student Login
            </Button>
            <Button
              variant="outline"
              onClick={() => setShowStaffDashboardPin(true)}
              className="bg-purple-600 hover:bg-purple-700 text-white border-0"
              title="Staff CRM Dashboard"
            >
              <LayoutDashboard className="h-5 w-5 mr-2" />
              <span className="hidden md:inline">Staff Dashboard</span>
            </Button>
            <Button
              variant="ghost"
              size="icon"
              onClick={() => setShowStaffAnalytics(true)}
              className="text-slate-400 hover:text-white"
              title="Staff Analytics"
            >
              <BarChart3 className="h-6 w-6" />
            </Button>
          </div>
        </div>
      </header>

      {/* Main Content */}
      <main className="flex-1 container mx-auto px-6 py-8">
        <div className="max-w-7xl mx-auto">
          {/* Welcome Message */}
          <div className="text-center mb-8">
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-3">
              Welcome to {APP_TITLE}
            </h2>
            <p className="text-lg md:text-xl text-slate-300">
              Select an option below to get started
            </p>
          </div>

          {/* Main Action Grid */}
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
            {mainActions.map((action) => {
              const Icon = action.icon;
              return (
                <Card
                  key={action.path}
                  className="group relative overflow-hidden border-slate-800 bg-slate-900/50 backdrop-blur-sm hover:bg-slate-900 transition-all duration-300 cursor-pointer hover:scale-105 hover:shadow-2xl"
                  onClick={() => navigate(action.path)}
                >
                  <div className={`absolute inset-0 bg-gradient-to-br ${action.color} opacity-0 group-hover:opacity-10 transition-opacity duration-300`} />
                  
                  {action.badge && (
                    <Badge className="absolute top-2 right-2 z-10 bg-red-600 text-white border-0">
                      {action.badge}
                    </Badge>
                  )}
                  
                  <div className="relative p-6 flex flex-col items-center text-center space-y-3">
                    <div className={`p-4 rounded-xl bg-gradient-to-br ${action.color} shadow-lg group-hover:scale-110 transition-transform duration-300`}>
                      <Icon className="h-8 w-8 md:h-10 md:w-10 text-white" />
                    </div>
                    
                    <div>
                      <h3 className="text-lg md:text-xl font-bold text-white mb-1">
                        {action.title}
                      </h3>
                      <p className="text-slate-400 text-sm md:text-base">
                        {action.description}
                      </p>
                    </div>
                  </div>
                </Card>
              );
            })}
          </div>
        </div>
      </main>

      {/* Footer */}
      <footer className="border-t border-slate-800 bg-slate-950/50 backdrop-blur-sm py-4">
        <div className="container mx-auto px-6 text-center text-slate-500 text-sm">
          <p>Powered by DojoFlow • Touch anywhere to begin</p>
        </div>
      </footer>

      {/* Kai AI Assistant Floating Button */}
      <div className="fixed bottom-6 right-6 z-50">
        <Button
          size="lg"
          onClick={() => setShowKai(!showKai)}
          className="h-16 w-16 rounded-full bg-gradient-to-br from-red-600 to-orange-600 hover:from-red-700 hover:to-orange-700 shadow-2xl hover:scale-110 transition-all duration-300"
        >
          <Bot className="h-8 w-8 text-white" />
        </Button>
      </div>

      {/* Kai Assistant Panel */}
      {showKai && (
        <div className="fixed inset-0 z-50 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4">
          <Card className="w-full max-w-2xl bg-slate-900 border-slate-800 shadow-2xl">
            <div className="p-6">
              <div className="flex items-center justify-between mb-6">
                <div className="flex items-center gap-3">
                  <div className="h-12 w-12 rounded-full bg-gradient-to-br from-red-600 to-orange-600 flex items-center justify-center">
                    <Bot className="h-7 w-7 text-white" />
                  </div>
                  <div>
                    <h3 className="text-2xl font-bold text-white">Kai Assistant</h3>
                    <p className="text-sm text-slate-400">How can I help you today?</p>
                  </div>
                </div>
                <Button
                  variant="ghost"
                  size="icon"
                  onClick={() => setShowKai(false)}
                  className="text-slate-400 hover:text-white"
                >
                  <X className="h-6 w-6" />
                </Button>
              </div>

              <div className="space-y-4">
                <div className="bg-slate-800/50 rounded-lg p-4 text-slate-300">
                  <p className="mb-4">👋 Hi! I'm Kai, your virtual assistant. I can help you with:</p>
                  <ul className="space-y-2 text-sm">
                    <li>• Checking in for class</li>
                    <li>• Registering for events and camps</li>
                    <li>• Finding class schedules</li>
                    <li>• Answering questions about programs</li>
                    <li>• Helping with payments and waivers</li>
                  </ul>
                </div>

                <div className="flex gap-2">
                  <Button
                    variant="outline"
                    className="flex-1 border-slate-700 text-white hover:bg-slate-800"
                    onClick={() => {
                      setShowKai(false);
                      navigate("/checkin");
                    }}
                  >
                    Check In
                  </Button>
                  <Button
                    variant="outline"
                    className="flex-1 border-slate-700 text-white hover:bg-slate-800"
                    onClick={() => {
                      setShowKai(false);
                      navigate("/events");
                    }}
                  >
                    View Events
                  </Button>
                </div>

                <p className="text-xs text-slate-500 text-center">
                  Voice assistance coming soon!
                </p>
              </div>
            </div>
          </Card>
        </div>
      )}

      {/* Staff Analytics PIN Modal */}
      {showStaffAnalytics && (
        <div className="fixed inset-0 z-50 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4">
          <Card className="w-full max-w-md bg-slate-900 border-slate-800 shadow-2xl">
            <div className="p-6">
              <div className="flex items-center justify-between mb-6">
                <h3 className="text-xl font-bold text-white">Staff Analytics</h3>
                <Button
                  variant="ghost"
                  size="icon"
                  onClick={() => {
                    setShowStaffAnalytics(false);
                    setStaffPin("");
                  }}
                  className="text-slate-400 hover:text-white"
                >
                  <X className="h-5 w-5" />
                </Button>
              </div>

              <div className="space-y-4">
                <div>
                  <label className="text-sm text-slate-400 mb-2 block">
                    Enter 4-digit staff PIN
                  </label>
                  <input
                    type="password"
                    maxLength={4}
                    value={staffPin}
                    onChange={(e) => setStaffPin(e.target.value.replace(/\D/g, ""))}
                    className="w-full px-4 py-3 bg-slate-800 border border-slate-700 rounded-lg text-white text-center text-2xl tracking-widest focus:outline-none focus:ring-2 focus:ring-red-600"
                    placeholder="••••"
                    autoFocus
                  />
                </div>

                <Button
                  className="w-full bg-gradient-to-r from-red-600 to-orange-600 hover:from-red-700 hover:to-orange-700"
                  disabled={staffPin.length !== 4}
                  onClick={() => {
                    if (staffPin.length === 4) {
                      setShowStaffAnalytics(false);
                      setStaffPin("");
                      navigate("/admin");
                    }
                  }}
                >
                  View Analytics
                </Button>

                <p className="text-xs text-slate-500 text-center">
                  Contact your manager if you don't have a PIN
                </p>
              </div>
            </div>
          </Card>
        </div>
      )}

      {/* Staff Dashboard PIN Modal */}
      {showStaffDashboardPin && (
        <div className="fixed inset-0 z-50 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4">
          <Card className="w-full max-w-md bg-slate-900 border-slate-800 shadow-2xl">
            <div className="p-6">
              <div className="flex items-center justify-between mb-6">
                <h3 className="text-xl font-bold text-white">Staff Dashboard Access</h3>
                <Button
                  variant="ghost"
                  size="icon"
                  onClick={() => {
                    setShowStaffDashboardPin(false);
                    setStaffDashboardPin("");
                  }}
                  className="text-slate-400 hover:text-white"
                >
                  <X className="h-5 w-5" />
                </Button>
              </div>

              <div className="space-y-4">
                <div>
                  <label className="text-sm text-slate-400 mb-2 block">
                    Enter 4-digit staff PIN
                  </label>
                  <input
                    type="password"
                    maxLength={4}
                    value={staffDashboardPin}
                    onChange={(e) => setStaffDashboardPin(e.target.value.replace(/\D/g, ""))}
                    className="w-full px-4 py-3 bg-slate-800 border border-slate-700 rounded-lg text-white text-center text-2xl tracking-widest focus:outline-none focus:ring-2 focus:ring-purple-600"
                    placeholder="••••"
                    autoFocus
                  />
                </div>

                <Button
                  className="w-full bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800"
                  disabled={staffDashboardPin.length !== 4 || isValidatingPin}
                  onClick={async () => {
                    if (staffDashboardPin.length === 4) {
                      setIsValidatingPin(true);
                      try {
                        const result = await validatePinMutation.mutateAsync({ pin: staffDashboardPin });
                        
                        if (result.valid) {
                          toast.success(`Welcome, ${result.staffName}!`);
                          setShowStaffDashboardPin(false);
                          setStaffDashboardPin("");
                          navigate("/crm-dashboard");
                        } else {
                          toast.error(result.message || "Invalid PIN");
                          setStaffDashboardPin(""); // Clear PIN on failure
                        }
                      } catch (error) {
                        toast.error("Failed to validate PIN. Please try again.");
                        setStaffDashboardPin("");
                      } finally {
                        setIsValidatingPin(false);
                      }
                    }
                  }}
                >
                  {isValidatingPin ? "Validating..." : "Access Dashboard"}
                </Button>

                <p className="text-xs text-slate-500 text-center">
                  Default PIN: 1234 (Change in settings)
                </p>
              </div>
            </div>
          </Card>
        </div>
      )}
    </div>
  );
}

